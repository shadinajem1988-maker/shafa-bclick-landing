# 🔄 תשתית אוטומציות — דף הנחיתה "שפה בקליק"

כל התשתית **בנויה ומוכנה בקוד** (רדומה). ההפעלה = הדבקת מזהים/כתובות ב-`CONFIG` שבתוך `index.html`. אין צורך לגעת בקוד.

---

## 🧠 איך זה עובד — מנוע `sendLead`
כל מילוי טופס קורא לפונקציה אחת, `sendLead(lead)`, ששולחת את הליד **לכל היעדים המוגדרים בו-זמנית**.
הליד כולל: שם · טלפון · מה מעניין · **מקור קמפיין (UTM)** · תאריך · מקור.
כל יעד שמוגדר ריק פשוט מדולג. בלי הגדרות = הכל רדום, ורק וואטסאפ נפתח (כמו היום).

```
        ┌──────────────────────────────┐
טופס →  │        sendLead(lead)        │
        └───────────┬──────────────────┘
                    ├─ ✉️  מייל מיידי (FormSubmit)          ← CONFIG.leadEmail
                    ├─ 📊  Google Sheet (מיני-CRM)          ← CONFIG.sheetWebhook
                    └─ 🔗  n8n / Make / Zapier (אוטומציה)   ← CONFIG.webhook
        ובמקביל → 💬 וואטסאפ נפתח עם הודעה מוכנה
```

---

## 🎛️ לוח ההפעלה (CONFIG ב-index.html)

| שדה | מה זה | מה צריך לעשות כדי להפעיל |
|-----|-------|--------------------------|
| `leadEmail` | מייל לכל ליד (FormSubmit) | **כבר מוגדר** (Safa.b.click@gmail.com). שלב הפעלה: שלח טופס בדיקה → לחץ Activate במייל שמגיע. |
| `sheetWebhook` | רישום אוטומטי לגיליון Google | פתח גיליון → הדבק את `google-apps-script.gs` → Deploy → הדבק את ה-URL כאן. |
| `webhook` | אוטומציה מלאה (CRM/בוט/תזכורות) | הקם Webhook ב-n8n/Make → הדבק את ה-URL כאן. |

### מעקב המרות (פיקסל) — נפרד, ב-`SBK_TRACK` שבראש ה-`<head>`
מלא `ga4` · `meta` · `clarity` · `userway`. יורה `Lead`/`Contact` בכל המרה (רק אחרי הסכמת עוגיות).

---

## 📊 הפעלת גיליון Google (כשתהיה מוכן)
ראה הוראות מלאות בתוך הקובץ `google-apps-script.gs`. בקצרה:
1. Google Sheet ריק → Extensions → Apps Script → הדבק את הקוד.
2. Deploy → Web app → Execute as: Me · Access: Anyone → אשר הרשאות.
3. העתק את ה-URL → הדבק ב-`CONFIG.sheetWebhook`.
תוצאה: כל ליד שורה חדשה (תאריך, שם, טלפון, מתעניין ב, קמפיין, מקור, **עמודת סטטוס** למעקב).

---

## 🔗 אוטומציה מלאה (השלב המתקדם — n8n/Make)
חבר את `CONFIG.webhook` ל-Webhook, ובכלי האוטומציה בנה:
```
Webhook → Google Sheet/CRM → הודעת ברוכים-הבאים בוואטסאפ
        → המתנה 24ש' → אם אין מענה: תזכורת → התראה ליועץ מכירות (05)
```
בוט סינון לידים: `20_מעבדת_AI/04_AI_שיחה_ובוטים/skills/whatsapp_bot.md`.

---

## ✅ צ'ק-ליסט הפעלה (כשתחליט לפרסם)
- [ ] FormSubmit: שלחתי טופס בדיקה ולחצתי Activate במייל
- [ ] Google Sheet: הדבקתי את ה-Apps Script, פרסמתי, והדבקתי URL ב-`sheetWebhook`
- [ ] (מתקדם) חיברתי `webhook` ל-n8n/Make לזרימה המלאה
- [ ] מילאתי 4 מזהי מעקב ב-`SBK_TRACK`
- [ ] עדכנתי `YOUR-DOMAIN` ב-robots.txt + sitemap.xml